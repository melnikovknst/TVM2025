Arithmetic {
    Expr = AdditiveExp
    AdditiveExp = TermExp (("+" | "-") TermExp)*  
    TermExp = Unary (("*" | "/") Unary)*   
    Unary = "-"* Atom
    Atom = Paren | number | variable
    Paren = "("AdditiveExp")"
    number = digit+
    variable = letter (letter | digit)*
}