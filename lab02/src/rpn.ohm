ReversePolishNotation {
	Expr = Stack
    Stack = Sum 	   --sum
    	| Mul 			--mul
    	| number		--num
    	|  Stack number --push
    Sum = Stack Stack "+"
    Mul = Stack Stack "*"
	number = digit+
}